import React from 'react';

const BtnFive = ({ children }) => {
   return (
      <div>
         <button className="custom-btn btn-5 ">{children}</button>
      </div>
   );
};

export default BtnFive;